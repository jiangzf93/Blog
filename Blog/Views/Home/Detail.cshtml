@model ArticleViewModel;

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>@Model.Article.Title</title>
    <link href="/lib/twitter-bootstrap/css/bootstrap.min.css" rel="stylesheet" />
</head>
<body>
    <div class="bg-dark text-white p-2 text-center">
        <span class="navbar-brand ml-2">@Model.Article.Title</span>
    </div>
    <div>
        <h5>作者：@(Model.Article.Author.AuthorName)</h5>
        <h3>@Model.Article.Text</h3>
    </div>
</body>
</html>